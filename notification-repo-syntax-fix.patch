--- a/server/src/Repositories/NotificationRepository.php	2026-07-01 20:01:48.000000000 +0000
+++ b/server/src/Repositories/NotificationRepository.php	2026-07-01 21:38:38.603861732 +0000
@@ -50,7 +50,7 @@
         $stmt->bindValue(':offset', $offset, PDO::PARAM_INT);
         $stmt->execute();
         $rows = $stmt->fetchAll(PDO::FETCH_ASSOC);
-        return array_map(fn($row) => {
+        return array_map(function ($row) {
             if ($row['data']) {
                 $row['data'] = json_decode($row['data'], true);
             }
